# Group 2.7 Hub: Inference and Compression Tracks | 2.7：推理与压缩双轨入口

本页是 `2.7A` 与 `2.7B` 的总入口。当前先把高级推理策略和模型压缩与量化拆成两条子线，便于后续补充新页面时保持结构清晰；等两条子线的模板和内容都稳定后，再决定是否整体重编号。

## Group Overview | 组概览

`2.7` 当前分成两条子线：

- `2.7A`：高级推理策略，先讲 `Speculative Decoding` 和 `RadixAttention`，后续再补 `Prefix Caching`、`Chunked Prefill`、`Multi-Token Decoding`、`Decode Scheduling`。
- `2.7B`：模型压缩与量化，先讲 `W8A16` 和 `QLoRA`，后续再补 `GPTQ`、`AWQ`、`FP8` 和 `KV Cache Quantization`。

## Page Template | 页面模板

`2.7A` 和 `2.7B` 后续新增页面都统一沿用 `22 / 23 / 24` 这一套 notebook 模板：

- 标题区：编号、标题、难度、环境、标签、目标人群、关键词。
- 前置阅读：只放真正服务正文主线的最小必要前置。
- 相关阅读：只放能帮助理解工程背景或后续扩展的链接。
- Step 1：先讲问题、痛点或核心机制。
- Step 2：再给实现框架或数据结构。
- Step 3：展开模拟机制或关键细节。
- Step 4：动手实战，给出最小可运行的代码骨架。
- STOP HERE：保留自练区，方便读者先自己补全。
- 参考代码与解析：最后给出答案和简短解析。

## Subgroup Entry | 子线入口

| 子线 | 主要内容 | 当前状态 | 入口 |
|:---|:---|:---|:---|
| `2.7A` | 高级推理策略 | 已有基础页面 | [2.7A](./2_7A.md) |
| `2.7B` | 模型压缩与量化 | 已有基础页面 | [2.7B](./2_7B.md) |

## Learning Path | 学习路径

### Recommended Order | 推荐顺序

- 先看 [2.7A](./2_7A.md)，把高级推理策略的主线先串起来。
- 再看 [2.7B](./2_7B.md)，把压缩与量化的主线接上。

### Next Steps | 后续衔接

- 看完这两条子线后，再进入 [2.8](./2_8.md)，把并行与通信边界接起来。
- 之后继续回到 [2.9](./2_9.md)，把前面的能力放进项目里验证。

## Extension Blueprint | 扩展蓝图

### 2.7A 扩展方向

- `36 Prefix Caching and Chunked Prefill`：补前缀缓存命中、切块预填充和复用机制。
- `37 Multi-Token Decoding`：补多 token 生成和草稿-验证链路。
- `38 Decode Scheduling`：补 prefill / decode 调度、批次排布和优先级规则。

### 2.7B 扩展方向

- `39 GPTQ and AWQ Weight Quantization`：补权重量化、校准和误差控制。
- `40 FP8 and KV Cache Quantization`：补 FP8 推理和 KV cache 量化。

## Environment Notes | 环境说明

- 当前这页只是总入口，不承担具体题目的展开。
- 组内资产会按 `2.7A` / `2.7B` 分别继续补充。
- 少数页面如需 `GPU optional` 或更完整工具链，以对应子线页面说明为准。
